interface:
  display_name: "Mineprogress: Create"
  short_description: "Create and bind a Kanban item"
  default_prompt: "Use $mineprogress:create to create and bind this Kanban item."
policy:
  allow_implicit_invocation: false
